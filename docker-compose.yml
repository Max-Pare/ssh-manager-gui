services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "6592:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "3001:3001"
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - PORT=3001
      - NODE_ENV=production
      - ALLOWED_ORIGIN=http://localhost:6592
    volumes:
      - ssh-mgr-data:/app/backend/data
      - ~/.ssh:/root/.ssh:ro
    restart: unless-stopped

volumes:
  ssh-mgr-data:
